# Admin Panel Monorepo

Monorepo для административной панели с микрофронтендами на Vite + Module Federation. Управление пакетами — pnpm, оркестрация задач — Turborepo.

## Содержание

- [Требования](#требования)
- [Установка](#установка)
- [Структура монорепозитория](#структура-монорепозитория)
- [Скрипты и команды](#скрипты-и-команды)
  - [Ежедневная разработка](#ежедневная-разработка)
  - [Микрофронтенды](#микрофронтенды)
  - [Сборка](#сборка)
  - [Тесты](#тесты)
  - [Линтинг и форматирование](#линтинг-и-форматирование)
  - [Storybook](#storybook)
- [Порты и URL](#порты-и-url)
- [Технологии](#технологии)
- [Новые инструменты и оптимизации](#новые-инструменты-и-оптимизации)
- [Архитектура](#архитектура)

## Требования

- Node.js 22+ (проект разрабатывался на 22.x; `apps/admin-shell` требует >= 22.12.0)
- pnpm 9+

Рекомендуется использовать версию Node из `.nvmrc` (если используете nvm/nvs/Volta).

## Установка

```bash
pnpm install
```

## Структура монорепозитория

- `apps/`
  - `admin-shell` — хост-приложение (shell)
  - `admin-statistics` — микрофронтенд «Статистика»
  - `admin-translations` — микрофронтенд «Переводы»
  - `admin-users` — микрофронтенд «Пользователи»
  - `docs` — Storybook
- `packages/`
  - `ui` — общий UI-kit
  - `lib` — общие утилиты, композаблы и т.д.
  - `i18n` - переводы
  - конфиги: `eslint-config`, `prettier-config`, `stylelint-config`, `typescript-config`

## Скрипты и команды

Все команды запускаются из корня репозитория.

### Ежедневная разработка

- Запуск всех приложений в dev-режиме (параллельно через Turborepo):

```bash
pnpm dev
```

- Запуск только shell (vite dev):

```bash
pnpm dev:shell
```

### Микрофронтенды

- Запуск микрофронтендов в preview и shell в dev одновременно:

```bash
pnpm dev:mf
```

Что делает команда:

- `preview:mf` — запускает preview для `apps/admin-statistics` и `apps/admin-translations`
- `dev:shell` — запускает dev для `apps/admin-shell`
- `run-p` запускает их параллельно (Windows-friendly)

- Отдельно только preview микрофронтендов:

```bash
pnpm preview:mf
```

Примечание: историческая команда `pnpm preview` была последовательной и могла «зависать» на первом preview; используйте `dev:mf`.

### Сборка

- Полная сборка всех пакетов и приложений:

```bash
pnpm build
```

- Сборка конкретного проекта (через интерактивный выбор):

```bash
pnpm build:project
```

### Линтинг и форматирование

- Запуск всех линтеров и проверки форматирования:

```bash
pnpm lint
```

- Автоисправления:

```bash
pnpm lint:fix
```

### Storybook

- Запуск Storybook (пакет `apps/docs`):

```bash
pnpm storybook
```

- Предпросмотр собранного Storybook:

```bash
pnpm preview-storybook
```

## Порты и URL

- Shell dev (Vite): `http://localhost:5173`
- `admin-statistics`
  - dev: `http://localhost:4174`
  - preview: `http://localhost:3001`
- `admin-translations`
  - dev: `http://localhost:4175`
  - preview: `http://localhost:3002`

Команда `pnpm dev:mf` поднимает shell в dev (5173) и микрофронтенды в preview (3001/3002) одновременно.

## Технологии

- Vite 7, Vue 3, Vue Router 4
- Module Federation (`@originjs/vite-plugin-federation`)
- Turborepo — параллельные dev/build задачи
- pnpm — workspaces и менеджер пакетов
- ESLint, Prettier, Stylelint
- Vitest, Cypress

## Новые инструменты и оптимизации

### 🚀 Улучшения производительности

- **Оптимизированная конфигурация Turborepo**: Улучшенное кэширование и зависимостей
- **Быстрый режим разработки**: Новая команда `pnpm dev:fast` для быстрого запуска без лишних сборок
- **Оптимизированные скрипты сборки**: Удалена лишняя сборка перед запуском dev:mf

### 🛠️ Новые инструменты

- **Dependency Cruiser**: Анализ зависимостей между пакетами

  ```bash
  pnpm why <package-name>  # Посмотреть почему пакет установлен
  pnpm outdated           # Посмотреть устаревшие зависимости
  pnpm dedupe             # Оптимизировать дерево зависимостей
  ```

- **Snyk**: Сканирование уязвимостей в зависимостях

  ```bash
  npx snyk test --severity-threshold=high
  ```

- **ESLint Security**: Проверка безопасности кода
  - Автоматическая проверка на уязвимости в коде

### 📊 Мониторинг и анализ

- **Turborepo Gen**: Генерация конфигураций Turborepo
- **Bundle Visualizer**: Анализ размеров бандлов (доступно через `pnpm dlx vite-bundle-visualizer`)

### 🔧 Улучшенные скрипты

```bash
# Анализ зависимостей
pnpm why <package-name>

# Проверка устаревших зависимостей
pnpm outdated

# Оптимизация дерева зависимостей
pnpm dedupe

# Быстрый запуск в режиме разработки (без admin-docs)
pnpm dev:fast

# Сборка только микрофронтендов
pnpm build:mf
```

### 🏗️ Архитектурные улучшения

- **Централизованная конфигурация TypeScript**: Новый корневой `tsconfig.json` для стандартных алиасов
- **Улучшенная структура кэширования**: Оптимизированные задачи в Turborepo
- **Лучшая изоляция микрофронтендов**: Улучшенные зависимости между задачами

## Архитектура

Подробная архитектура проекта Подробности в [admin-panel/TESTING.md](TESTING.md) и [server/TESTING.md](../server/TESTING.md).

Для визуализации текущей структуры зависимостей используйте:

```bash
npx dependency-cruiser --validate .
```

## CI/CD

Проект теперь поддерживает автоматизированные workflowы:

- **CI Pipeline**: Автоматическая проверка PR (линтинг, типы, сборка, тесты, безопасность)
- **CD Pipeline**: Автоматическое развертывание по тегам

Смотрите конфигурации в `.github/workflows/`.

---

Если требуется добавить новые микрофронтенды — используйте аналогичную конфигурацию `vite` и добавьте соответствующие `scripts` (`dev`, `build`, `preview`), после чего включите их в корневые фильтры `preview:mf`/`dev:mf` при необходимости.

---

Если требуется добавить новые микрофронтенды — используйте аналогичную конфигурацию `vite` и добавьте соответствующие `scripts` (`dev`, `build`, `preview`), после чего включите их в корневые фильтры `preview:mf`/`dev:mf` при необходимости.
