# app

# APP_HOST= if docker: 0.0.0.0 else 127.0.0.1
APP_HOST=
APP_PORT=
APP_DB_ENGINE=
PUBLIC_URL=http://
CORS_ALLOWED_ORIGINS=http://localhost:8000,http://localhost:5500
REFRESH_TOKEN_TTL_MINUTES=10080  # 7 дней = 7 * 24 * 60 = 10080 минут

# postgres
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_PORT=
POSTGRES_HOST=
# POSTGRES_HOST= if docker: db else localhost
